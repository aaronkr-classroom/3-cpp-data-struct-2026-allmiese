// quartile.cpp
/*
25%
50%
75%
*/
#include <iostream>
#include <vector>
#include <algorithm> // sort()

using namespace std;

typedef vector<int>::size_type vec_sz;

int quartile_calc (vector<int> data, int quartile) {
    int size = data.size();
    vec_sz q = size * (quartile / 100);

    double median;
    median = size % 2 == 0 ? 
       (homework[mid_pt] + homework[mid_pt - 1]) / 2: // if
       homework[mid_pt]; // else

}

int main (void) {

    cout << "Enter all values, the EOF: "

    vector <int> val;
    int in;

    while (cin >> in) {
        val.push_back(in);
    }

    vec_sz size = val.size();
    if (size == 0){
        cout << "NO values. Try again!" << endl;
        return 1;
    }

    sort (val.begin(), val.end());

    void print_quartile(vector<int> data) {
    sort(data.begin(), data.end());

    vec_sz q1 = quartile_calc (data, 25);  #First quartile (25%)
    vec_sz q2 = quartile_calc (data, 50);  #Second quartile (median, 50%)
    vec_sz q3 = quartile_calc (data, 75);  #Third quartile (75%)

    int q1_val, q2_val, q3_val;
    q1_val = size % 4 == 0 ? (val[q1] + val[q1 - 1]) / 2 : val[q1];
    q2_val = size % 2 == 0 ? (val[q2] + val[q2 - 1]) / 2 : val[q2];
    q3_val = 3 * size % 4 == 0 ? (val[q3] + val[q3 - 1]) / 2 : val[q3];


    cout <<
    "Q1: " << q1_val << endl <<
    "Q2: " << q2_val << endl <<
    "Q3: " << q3_val << endl;
}

    vec_sz mid_pt = size / 2;
    double median;
    median = size % 2 == 0 ? 
       (homework[mid_pt] + homework[mid_pt - 1]) / 2: // if
       homework[mid_pt]; // else
 
return 0;
}